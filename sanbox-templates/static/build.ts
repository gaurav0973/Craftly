// Static sandbox setup — for reference only.
// The sandbox is created dynamically in features/inngest/functions.ts
// using Sandbox.create({ template: "base" }) and then:
//   1. npm install -g serve
//   2. Scaffold /home/user/index.html, style.css, script.js, pages/
//   3. nohup serve /home/user -l 3000 --no-clipboard &
//
// No pre-built E2B template is needed for this static HTML approach.
